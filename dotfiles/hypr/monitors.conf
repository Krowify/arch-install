# Deployed by 5-dotfiles.sh, but only the first time -- once this file
# exists, 5-dotfiles.sh preserves it as-is on every future redeploy
# instead of overwriting it, so a layout saved by ./hypr-monitor-layout.sh
# survives re-running the installer. Sourced from hyprland.conf. See the
# README's "Multi-monitor layout" section.
monitor = , preferred, auto, auto
