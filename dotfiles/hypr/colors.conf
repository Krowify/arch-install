# Deployed by 5-dotfiles.sh. Static fallback palette (Catppuccin Mocha +
# custom accents, see README credits) -- this file gets overwritten
# in-place by Matugen once you pick a wallpaper via waypaper (see the
# README's color theming section). Sourced from hyprland.conf.

$colActiveBorder1 = rgba(b072d1ee)
$colActiveBorder2 = rgba(3fc6deee)
$colInactiveBorder = rgba(44465caa)
