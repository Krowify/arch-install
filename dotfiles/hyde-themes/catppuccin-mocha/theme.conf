# Deployed by 5-dotfiles.sh into ~/.config/hyde-themes/catppuccin-mocha/.
# Read by theme.sh. GTK/icon themes are already installed by stage 3
# (catppuccin-gtk-theme-mocha, papirus-icon-theme AUR/pacman packages), so
# no *_ASSET_URL is needed here -- theme.sh only needs to find them.

GTK_THEME_GLOB="Catppuccin-Mocha-Standard-Mauve*"
GTK_THEME_FALLBACK_GLOB="Catppuccin-Mocha*"
ICON_THEME_GLOB="Papirus-Dark"
ICON_THEME_FALLBACK_GLOB="Papirus*"
