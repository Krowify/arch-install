# Deployed by theme.sh into ~/.config/hypr/colors.conf. Decay Green
# palette -- this repo's own construction, not a verified port (see the
# disclaimer in theme.conf in this same directory). Not touched by
# Matugen: picking a new wallpaper via waypaper still re-themes
# everything dynamically (that's unchanged), but theme.sh itself sets
# these directly rather than deriving them from a wallpaper.

$colActiveBorder1 = rgba(8fbc6cff)
$colActiveBorder2 = rgba(6fae95ff)
$colInactiveBorder = rgba(445228cc)
$colInactiveBorder2 = rgba(6d7a55cc)
